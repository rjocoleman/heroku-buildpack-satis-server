#!/usr/bin/env bash
# bin/release <build-dir>

# Serve satis up
cat <<EOF
---
addons:
  scheduler:standard
config_vars:
  GITHUB_API_KEY:
  PATH: "vendor/satis/bin:vendor/bin:vendor/php/bin:/usr/local/bin:/usr/bin:/bin"
default_process_types:
  web: php -S 0.0.0.0:\$PORT -t web/
EOF
